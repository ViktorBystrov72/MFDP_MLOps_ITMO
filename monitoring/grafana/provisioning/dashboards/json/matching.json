{
  "annotations": {"list": []},
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {"unit": "reqps"}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 0, "y": 0},
      "id": 1,
      "targets": [
        {
          "expr": "rate(matching_requests_total[5m])",
          "legendFormat": "{{endpoint}}",
          "refId": "A"
        }
      ],
      "title": "Match requests rate",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 8, "y": 0},
      "id": 2,
      "targets": [
        {
          "expr": "sum by (result) (rate(matching_pairs_total[5m]))",
          "legendFormat": "{{result}}",
          "refId": "A"
        }
      ],
      "title": "Pairs by result",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {"unit": "s"}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 16, "y": 0},
      "id": 3,
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(matching_latency_seconds_bucket[5m])) by (le))",
          "legendFormat": "p95",
          "refId": "A"
        }
      ],
      "title": "Inference latency p95",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8},
      "id": 4,
      "targets": [
        {
          "expr": "sum by (status) (increase(matching_llm_calls_total[1h]))",
          "legendFormat": "{{status}}",
          "refId": "A"
        }
      ],
      "title": "LiteLLM calls (1h)",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {"max": 1, "min": 0}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8},
      "id": 5,
      "targets": [
        {
          "expr": "matching_model_loaded",
          "legendFormat": "catboost",
          "refId": "A"
        }
      ],
      "title": "Model loaded",
      "type": "stat"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 0, "y": 16},
      "id": 6,
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(matching_candidates_per_deal_bucket[15m])) by (le))",
          "legendFormat": "p95 candidates",
          "refId": "A"
        }
      ],
      "title": "Candidates per deal",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {"min": 0, "max": 1}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 8, "y": 16},
      "id": 7,
      "targets": [
        {
          "expr": "histogram_quantile(0.5, sum(rate(matching_top1_margin_bucket[15m])) by (le))",
          "legendFormat": "p50",
          "refId": "A"
        },
        {
          "expr": "histogram_quantile(0.95, sum(rate(matching_top1_margin_bucket[15m])) by (le))",
          "legendFormat": "p95",
          "refId": "B"
        }
      ],
      "title": "Top-1 margin",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 16, "y": 16},
      "id": 8,
      "targets": [
        {
          "expr": "sum by (signal, status) (increase(matching_inputs_total[1h]))",
          "legendFormat": "{{signal}} / {{status}}",
          "refId": "A"
        }
      ],
      "title": "Critical input availability",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {"unit": "currencyUSD"}, "overrides": []},
      "gridPos": {"h": 6, "w": 8, "x": 0, "y": 24},
      "id": 9,
      "targets": [
        {
          "expr": "matching_llm_cost_dollars_total",
          "legendFormat": "cost",
          "refId": "A"
        }
      ],
      "title": "LiteLLM accumulated cost",
      "type": "stat"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {"unit": "s"}, "overrides": []},
      "gridPos": {"h": 6, "w": 8, "x": 8, "y": 24},
      "id": 10,
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum by (le, strategy) (rate(location_matching_latency_seconds_bucket[15m])))",
          "legendFormat": "{{strategy}} p95",
          "refId": "A"
        }
      ],
      "title": "Deal→building model latency",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {"min": 0, "max": 1}, "overrides": []},
      "gridPos": {"h": 6, "w": 8, "x": 16, "y": 24},
      "id": 11,
      "targets": [
        {
          "expr": "location_matching_models_loaded",
          "legendFormat": "BGE + reranker",
          "refId": "A"
        }
      ],
      "title": "Location models loaded",
      "type": "stat"
    }
  ],
  "schemaVersion": 39,
  "tags": ["matching", "neolithic"],
  "templating": {"list": []},
  "time": {"from": "now-6h", "to": "now"},
  "title": "Exposition-Deal Matching",
  "uid": "matching-overview",
  "version": 1
}
